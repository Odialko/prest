class LayoutsConstants {
  const LayoutsConstants._();

  static const maxContentWidth = 1440.0;
  static const brakePointToMobile = 800.0;

  static const horizontalPaddingDesktop = 60.0;
  static const horizontalPaddingMobile = 20.0;

  static const headerHeight = 120.0;
  static const animationHeaderDuration = Duration(milliseconds: 500);

  static const footerVerticalPadding = 80.0;

  static const hoverMenuWidth = 220.0;
  static const hoverMenuOffset = 55.0; // Adjusted for seamless bridge
  static const hoverMenuHoverOpacity = 0.3;
  static const hoverMenuAnimationDuration = Duration(milliseconds: 300);
  static const hoverMenuItemAnimationDuration = Duration(milliseconds: 200);
  static const hoverMenuExitDelay = Duration(milliseconds: 200);
}